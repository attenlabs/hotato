# Environment for the generated hotato webhook worker ({{STACK}}).
# Copy to .env and fill in. Never commit real secrets.

{{ENV_VARS_BLOCK}}

# Where candidate reports are written (JSON + HTML).
HOTATO_REPORT_DIR=reports

# --- optional: Slack summary (leave unset to disable) ---
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# --- optional: GitHub notification (OFF by default; opens no issue unless you opt in) ---
# HOTATO_GITHUB_NOTIFY=1
# HOTATO_GITHUB_CREATE_ISSUES=1
# GITHUB_REPOSITORY=owner/name
# GITHUB_TOKEN=ghp_...
