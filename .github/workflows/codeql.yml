# Static analysis (CodeQL, python). Scans on main pushes that touch Python,
# every PR, and a weekly sweep. Read-only except the security-events write
# the upload step requires.
name: codeql
on:
  push:
    branches: [main]
    paths: ["**/*.py"]
  pull_request:
    paths: ["**/*.py"]
  schedule:
    - cron: "17 6 * * 1"
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: github/codeql-action/init@7188fc363630916deb702c7fdcf4e481b751f97a # v3.30.6
        with:
          languages: python
      - uses: github/codeql-action/analyze@7188fc363630916deb702c7fdcf4e481b751f97a # v3.30.6
