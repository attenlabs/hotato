{
  "id": "gld-quiet-caller-invisible",
  "title": "CAPTURE DEFECT RENDER: caller below the absolute gate (must FAIL)",
  "category": "should_yield",
  "tags": [
    "interruption",
    "gain",
    "capture-defect"
  ],
  "family": "capture-defect",
  "source_type": "synthetic",
  "sample_rate": 16000,
  "duration_sec": 5.0,
  "caller_onset_sec": 2.0,
  "expected": {
    "yield": true,
    "max_time_to_yield_sec": 1.0,
    "max_talk_over_sec": 1.15
  },
  "reference_render": {
    "agent_segments_sec": [
      [
        0.2,
        2.5
      ]
    ],
    "caller_segments_sec": [
      [
        2.0,
        4.2
      ]
    ],
    "caller_gain": 0.003
  },
  "reference_verdict": "fail",
  "failure_axis": "barge_in",
  "why_it_matters": "CAPTURE DEFECT RENDER. The caller channel is scaled to 0.003x, below the scorer's -60 dBFS absolute gate; the rendered yield cannot be attributed to the caller and the fixture must fail. Fix the capture level, not the agent.",
  "related_signals": [
    "did_yield",
    "time_to_yield",
    "talk_over"
  ]
}
