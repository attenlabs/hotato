{
  "id": "mup-pause-jumpin",
  "title": "DEFECT RENDER: agent grabs the floor inside the caller's pause (latency must FAIL)",
  "category": "latency",
  "tags": [
    "latency",
    "endpointing",
    "pause",
    "thinking-pause",
    "bad-agent"
  ],
  "family": "mid-utterance-pause",
  "source_type": "synthetic",
  "sample_rate": 16000,
  "duration_sec": 8.5,
  "caller_onset_sec": 1.6,
  "expected": {
    "yield": true,
    "max_time_to_yield_sec": 0.7,
    "max_talk_over_sec": 0.8
  },
  "latency_bounds": {
    "max_response_gap_sec": 1.0,
    "premature_is_failure": true,
    "boundary_tolerance_hops": 1,
    "turn_end_silence_sec": 2.6,
    "note": "Scored with a ScoreConfig(turn_end_silence_sec=2.60) wider than the 2.00s pause, so the pause is not mistaken for the caller's true turn end. tests/test_corpus_classes.py is the only place that config is applied; the barge-in verdict is a separate axis and the reference agent yields cleanly."
  },
  "reference_render": {
    "continuous": true,
    "agent_segments_sec": [
      [
        0.2,
        1.4
      ],
      [
        3.3,
        4.8
      ]
    ],
    "caller_segments_sec": [
      [
        1.6,
        2.6
      ],
      [
        4.6,
        5.6
      ]
    ],
    "caller_offset_sec": 5.6,
    "agent_response_onset_sec": 3.3,
    "pause_gap_sec": 2.0,
    "rendered_premature_lead_sec": 2.3
  },
  "reference_verdict": "fail",
  "failure_axis": "latency",
  "why_it_matters": "DEFECT RENDER. The agent starts talking 0.7s into the caller's 2.0s thinking pause, before the caller resumes and finishes; a premature grab of the floor mid-thought.",
  "related_signals": [
    "premature_start_sec",
    "did_yield"
  ]
}
