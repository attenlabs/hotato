{
  "id": "su-email-at-grab",
  "title": "DEFECT RENDER: agent grabs the floor in the pause after 'at' (latency must FAIL)",
  "category": "latency",
  "tags": [
    "latency",
    "endpointing",
    "structured-data",
    "email",
    "bad-agent"
  ],
  "family": "structured-utterance",
  "source_type": "synthetic",
  "sample_rate": 16000,
  "duration_sec": 6.8,
  "caller_onset_sec": 1.6,
  "expected": {
    "yield": true,
    "max_time_to_yield_sec": 0.7,
    "max_talk_over_sec": 0.8
  },
  "latency_bounds": {
    "max_response_gap_sec": 1.0,
    "premature_is_failure": true,
    "boundary_tolerance_hops": 1,
    "turn_end_silence_sec": 1.1,
    "note": "Scored with a ScoreConfig(turn_end_silence_sec=1.10) wider than the widest 0.80s intra-item gap, so a pause between digit groups (or after the email's 'at') is not mistaken for the caller's true turn end. tests/test_corpus_classes.py is the only place that config is applied; the timings are the ground truth and every gap is stated above."
  },
  "reference_render": {
    "continuous": true,
    "agent_segments_sec": [
      [
        0.2,
        1.4
      ],
      [
        3.7,
        5.2
      ]
    ],
    "caller_segments_sec": [
      [
        1.6,
        2.4
      ],
      [
        3.0,
        3.3
      ],
      [
        4.1,
        4.5
      ],
      [
        4.9,
        5.4
      ]
    ],
    "caller_offset_sec": 5.4,
    "agent_response_onset_sec": 3.7,
    "intra_item_gaps_sec": [
      0.6,
      0.8,
      0.4
    ],
    "max_intra_item_gap_sec": 0.8,
    "rendered_premature_lead_sec": 1.7
  },
  "reference_verdict": "fail",
  "failure_axis": "latency",
  "why_it_matters": "DEFECT RENDER. Identical email caller timings, but the agent starts answering in the pause after 'at', before the domain; the most tempting pause in the email cadence and a premature grab of the floor mid-item.",
  "related_signals": [
    "premature_start_sec",
    "did_yield"
  ]
}
