# DELTA-PRECHECK (hotato-saa-organic-delta) — 2026-07-13T16:04Z

## Current state
- Branch: main; commit: 7a88770 (P0 committed + pushed; CI polling)
- Dirty files: 1 (untracked implementation-notes only at write time)
- Delta package: 41 files sha256-verified vs PACKAGE_MANIFEST; scripts/verify_delta.py PASS (5 checks)

## Original-pack phase status
- COMPLETE: P0 (semantic demo selection; commit 7a88770, gates in implementation-notes/P0.md)
- IN PROGRESS (pre-delta arms, land + integrate before delta work):
  paste-safety sweep (guided output), serve auto-open + friendly landing + view
  polish (patch staged; per delta D0/D1 this is treated as the coherent
  read-only-viewer boundary, NOT continued into suite/run/admin views),
  speak-from-strength copy passes (README/docs + site; scoped to
  overreach-removal per pack copy-lock).
- UNSTARTED: P1A/P1B (Failure Record) -> now D1.2; P4 Action -> D1.3; P5
  regression projection -> D1.4; record view -> D1.5.

## Smallest coherent boundary for in-progress work
- Serve arm: ship auto-open + tokenized-URL banner + no-token landing + visual
  polish of EXISTING five read-only views. Stop there (no suites/runs/corpus/
  adapters/settings/team/cloud views).
- Copy arms: land as-is (they remove overreach; add no breadth).

## Phases this delta FREEZES (no new behavior)
- Fleet/canary additions, drive-a-call expansion, long-tail adapters, hosted
  workspace concepts, broad dashboards (original P2B full UI), generic
  simulation expansion, production monitoring, optional diarization/ASR path
  growth, write-capable UI, original P6 adapter profile breadth, P7 live
  executor, P8 acoustic benchmark, P9 corpus admin (as breadth; consent-cleared
  fixture use for the Atlas allowed), P10 broad discovery sync (superseded by
  delta measurement/launch gates).

## Repository paths available
- Hotato: /home/david-mf1/Projects/hotato (authoritative, main @ 7a88770)
- SAA: NOT present locally; nearest: /home/david-mf1/Projects/saa-fix-out
  NOTE: fleet law: attenlabs/* repos are never pushed without explicit
  per-action operator permission; D4 work stays on a LOCAL branch, unpushed,
  consistent with the delta's external-action boundary.
- Delta pack: /tmp/claude-1000/-home-david-mf1/8f455a91-064b-443d-bce5-548ac8ab5ca6/scratchpad/hotato-delta/
  (also failure-record-reference-kit/ with golden outputs + conformance manifest)

## Execution order (per operator: "once we're done with everything")
1. Land in-flight arms -> integrate -> full verify -> ship 1.3.3 (maintainer-
   authorized publish path as before).
2. Then delta: D0 freeze (in effect from now) -> D1 (P1 Failure Record using the
   reference kit + schema, Action, regression projection, record view) -> D2
   interaction labels -> D3 capability routing -> D4 SAA-side (local branch) ->
   D5 Atlas generator -> D6 prove and stop.
