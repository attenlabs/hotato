# Phase P0 gate report

## Baseline
- Branch: main
- Starting commit: 544a7d1 (ahead of pack baseline 25504ff; defect re-verified on HEAD)
- Ending commit: (this commit)
- Package version: 1.3.2
- Worktree before: clean (implementation-notes/ untracked)
- Worktree after: start.py + 2 test files + notes only

## Inspection
- Files read: src/hotato/start.py, src/hotato/contract.py (create/verify + scorable
  gates), src/hotato/fixture.py (#N candidate-ref resolution), src/hotato/analyze.py
  (_sort_key: rank = pure salience artifact), src/hotato/scan.py (candidate kinds),
  src/hotato/data/demo/failing/scenarios/*.json, tests/test_start_cli.py.
- Existing behavior: _DEMO_CONTRACT_CANDIDATE = 2 (positional). Live sweep rank 2 =
  agent_stop_no_caller: no caller onset, so the "agent talked over the caller" demo
  story verified with measurement {did_yield: False, seconds_to_yield: None,
  talk_over_sec: 0.0}. Reproduced on HEAD and on a clean macOS 1.3.2 install.
- Compatibility constraints: demo exit contract (0), printed output ordering, JSON
  envelope keys preserved. Only the selected candidate changes.
- Security/privacy boundary: none crossed (local packaged audio only).

## Reproducer or contract test
Command:
```text
PYTHONPATH=src python3 -m pytest tests/test_start_demo_selection.py -q   # pre-fix
```
Unedited output:
```text
FAILED tests/test_start_demo_selection.py::test_demo_contract_is_built_from_the_declared_missed_interruption
FAILED tests/test_start_demo_selection.py::test_selector_is_stable_under_reordering
FAILED tests/test_start_demo_selection.py::test_selector_zero_matches_is_a_distinct_internal_error
FAILED tests/test_start_demo_selection.py::test_selector_multiple_matches_is_a_distinct_internal_error
FAILED tests/test_start_demo_selection.py::test_selector_ignores_malformed_candidates
FAILED tests/test_start_demo_selection.py::test_repeated_demo_runs_select_the_same_event
6 failed in 1.64s
```
Expected failure reason: selector absent; positional rank 2 selects an
agent_stop_no_caller event instead of the declared missed interruption.

## Changes
- Files changed: src/hotato/start.py (semantic selector _select_demo_candidate +
  _demo_scenario + DemoSelectionError/DemoCandidateNotFound/DemoCandidateAmbiguous;
  scorability is part of the internal contract, fail-loud; defensive except no
  longer swallows DemoSelectionError); tests/test_start_demo_selection.py (new, 6
  tests: e2e-declared-event, reordering, zero-match, duplicate-match, malformed,
  repeated-run); tests/test_start_cli.py (stale #2 docstring removed).
- Public schema change: none. CLI change: none (same commands/keys). Packaging: none.
- Documentation change: none (P0 forbids copy changes).

## Verification
| Gate | Command | Exit | Result |
|---|---|---:|---|
| Targeted tests | pytest tests/test_start_demo_selection.py -q | 0 | 6 passed |
| Adjacent tests | pytest test_start_cli test_demo_cli test_fixture_promote test_contract_cli -q | 0 | 137 passed |
| Full tests | pytest -q (background, tail below) | 0 | see suite line |
| Offline | demo runs in scratch dir, packaged audio only | 0 | pass |
| Compatibility | text + JSON demo runs agree (scorable true, passed false) | 0 | pass |

## Acceptance gate evidence
- P0.semantic_selection: test_selector_is_stable_under_reordering (reorder chooses
  the same (source, kind, t_sec); rank unused).
- Zero/multiple distinct errors: DemoCandidateNotFound vs DemoCandidateAmbiguous,
  both under DemoSelectionError; asserted distinct.
- No rank-2 dependency: grep clean; only the regression-test docstring documents
  the historical defect.
- Trust+measurement scorable: e2e test asserts scorable is True and
  measurement.talk_over_sec > 0 (was 0.0 pre-fix); create path refuses
  not-scorable; verify path re-checked and fail-loud on mismatch.
- CLI/JSON/report/exit agree: both formats show scorable true + passed false; demo
  exits 0 (setup succeeded), contract verify exits 1 in CI as documented.
- Mono/malformed unscorable: selector skips malformed candidates
  (test_selector_ignores_malformed_candidates); mono input remains gated by the
  existing analyze/contract not-scorable path (unchanged).
- Repeated-run stability: test_repeated_demo_runs_select_the_same_event.

## Suite notes
- Full-suite run showed 2 non-P0 failures: test_ollama_judge_live (live local
  Ollama test; fails identically on the parent commit under load, passes in
  isolation, skipped in CI: pre-existing environmental flake) and
  test_no_unguarded_opens (introduced by this change's raw open(); fixed in the
  amended commit by routing through errors.open_regular; test green).

## Known limitations
- Selector matches source filename + kind + onset window from the packaged
  scenario declaration; source digest pinning deferred to P1 evidence work.
- start --demo next-steps still reference candidate #1 for promote/card examples
  (user-choice examples, out of P0 scope; revisit in P3 demo v2).

## Claims enabled
- The first-run demo contract is built from the declared missed interruption and
  verifies with a measured talk-over (0.25s), matching the printed story.

## Claims still unsupported
- Acoustic environments in the 375-run matrix; interactive simulation (P7/P8).

## Rollback
- Revert this commit; no schema/data migration involved.

## Commit
- Hash: 4e57cac
- Message: fix(demo): select the demonstration event by evidence
